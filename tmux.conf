# vim: set ft=conf:
new-window -n ${mxl_cwdname}
send-keys -t: 'vim .' C-m
split-window -h -t:
send-keys -t: 'git status' C-m
split-window -v -t:
send-keys -t: 'couchdb' C-m
split-window -v -t:
send-keys -t: 'npm test' C-m
select-pane -L
